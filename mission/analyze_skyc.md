## 任务规划

1. 分析 `skybrush` 的 `blender` 插件

* `skybrush` 中舞步生成使用的什么算法
* 舞步生成的算法流程
* `.skyc` 格式的文件是如何生成的
* `.skyc` 格式的文件如何在 `c++` 中解包
* 一场表演所有的无人机的舞步都在一个 `.skyc` 文件下，如何由这一个舞步生成每一个无人机单独的舞步文件（其后缀也是 `.skyc`）

2. 写我们自己的插件

* 用到什么算法
* 

## 1、分析 `skybrush` 插件







## 2、23232

